export function TrainingVideoPlayer({ videoUrl }) {
  // This is a placeholder. A real implementation would use a video player library.
  return (
    <div>
      <h3>Training Video</h3>
      <video src={videoUrl} controls />
    </div>
  );
}
